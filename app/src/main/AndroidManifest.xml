<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.madinfotech.orderbox">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".OrderBoxApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/LoginAppTheme">
        <activity android:name=".ui.screens.LoginActivity"
            android:theme="@style/LoginAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.screens.DashboardActivity">

        </activity>

        <activity android:name=".ui.screens.AddTaskActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>